{
  "name": "gemini-watermark-remover-light",
  "version": "0.2.5",
  "private": true,
  "description": "Lightweight local remover for the visible Gemini image watermark, available as a userscript and browser web tool.",
  "type": "module",
  "scripts": {
    "check": "node --check gemini-watermark-remover-light.user.js && node --check web/app.js",
    "test": "node --test tests/*.test.mjs"
  },
  "license": "MIT"
}
