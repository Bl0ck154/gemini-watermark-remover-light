{
  "name": "gemini-watermark-remover-light",
  "version": "0.2.5",
  "private": true,
  "description": "Lightweight download-focused userscript for removing visible Gemini image watermarks.",
  "type": "module",
  "scripts": {
    "check": "node --check gemini-watermark-remover-light.user.js && node --check docs/app.js && node --check scripts/sync-docs-maps.mjs && node scripts/sync-docs-maps.mjs --check",
    "sync:docs-maps": "node scripts/sync-docs-maps.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "license": "MIT"
}
