{
  "name": "gemini-watermark-remover-light",
  "version": "0.3.0",
  "private": true,
  "description": "Lightweight local browser tools and userscript for removing visible Gemini image watermarks, with experimental Video Light support.",
  "type": "module",
  "scripts": {
    "check": "node --check gemini-watermark-remover-light.user.js && node --check docs/app.js && node --check docs/video.js && node --check docs/video-bootstrap.js && node --check scripts/sync-docs-maps.mjs && node scripts/sync-docs-maps.mjs --check",
    "sync:docs-maps": "node scripts/sync-docs-maps.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "license": "MIT"
}
