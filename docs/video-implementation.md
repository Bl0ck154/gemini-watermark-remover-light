Video Light v1 is implemented as a static GitHub Pages browser tool. Processing remains local; media is not uploaded. The engine uses Mediabunny for MP4 demux/mux, WebCodecs for AVC output, existing alpha calibration data, multi-frame detection, confidence gates, bounded alpha adjustment, and optional soft residual cleanup. ML denoise is deferred to a future lazy-loaded enhanced mode.